# FreshGrad Tracker Environment Variables
# Copy this file to .env for local development

# Frontend API URL (Backend server URL)
VITE_API_URL=http://localhost:8080/api

# Database connection (PostgreSQL)
# For local development, install PostgreSQL or use a cloud provider
DATABASE_URL=postgresql://username:password@localhost:5432/freshgrad

# Node environment
NODE_ENV=development

# Server port (optional, defaults to 8080)
PORT=8080
